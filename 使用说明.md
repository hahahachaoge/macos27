# macOS 27 “Mammoth” · 使用说明（中文）

> 🌐 **语言 / Language：** [中文](使用说明.md) · [English](使用说明_EN.md)

这是一个完全运行在浏览器里的 macOS 27 桌面系统：真正的 **Liquid Glass（液态玻璃）** 材质、
14 个“原生”应用、以及由 **DeepSeek V4 Pro** 驱动的内置智能助手 Intelligence。
无需安装任何东西，没有框架、没有外部素材、没有构建步骤。

## ✨ 完善版新增功能速览

- **窗口**：绿色按钮/`F11` 真·全屏（菜单栏与 Dock 自动隐藏、鼠标顶边滑出）；`⌘H` 隐藏应用；拖动分屏吸附；**窗口布局重启自动恢复**；多桌面 Spaces（`Ctrl+←/→`、Mission Control 里「+」新建）。
- **菜单栏**：菜单支持键盘方向键导航；Edit 菜单真实剪贴板（撤销/剪切/复制/粘贴）。
- **通知中心**：点菜单栏时钟打开，含今日日程 + 通知列表 + 勿扰 + 清空，时钟带未读红点。
- **Dock**：图标可拖动排序、右键保留/移除、最近使用区。
- **Spotlight**：单位换算（`10 km to miles`）、全文内容搜索、快捷操作、搜索历史。
- **Finder**：分栏视图、空格键快速查看、全盘搜索、颜色标签、最近使用、复制/剪切/粘贴、多选。
- **Safari**：书签/历史/阅读列表侧栏、隐私模式。**Notes**：富文本、文件夹、导出、锁定。
- **Mail**：草稿自动保存、搜索、转发、VIP。**Messages**：搜索、删除、Tapback 回应、图片。
- **Calendar**：月/周/日三视图、事件编辑、全天、重复、搜索。**Photos**：相簿/为你推荐/搜索/编辑。
- **Music**：随机/循环、收藏、三频段均衡器。**计算器**：科学模式、历史。**终端**：多会话标签、管道/重定向、`grep/find/wc` 等。
- **系统设置**：新增 Wi-Fi / 蓝牙 / 显示器 / 通知 / 键盘 / 触控板 / 电池 / Apple 账户面板。
- **Intelligence**：多会话历史、停止生成、代码复制、模型/温度切换、回复延迟显示。

---

## 一、如何启动

**方式一（最简单）**：直接双击打开 `index.html` 即可，无需任何服务器。

**方式二（推荐）**：用项目自带的零依赖服务器：

```bash
cd macos27
node serve.js
```

然后打开浏览器访问：**http://localhost:8080**

**方式三**：用你已有的任何静态服务器：

```bash
python -m http.server 8080
# 或
npx serve .
```

> 浏览器要求：推荐最新版 **Chrome / Edge / Safari**（需要 `backdrop-filter` 和 SVG 滤镜支持）。
> Firefox 大部分可用，但个别玻璃路径的背板着色效果会降级。

---

## 二、开机流程

1. **开机画面** —— 苹果 Logo + 进度条（约 3 秒）。
2. **锁定屏幕** —— 显示大时钟与日期。点击任意位置（或按回车 / 空格）解锁，同时响起解锁音效。
3. **首次进入桌面** —— 会自动弹出「Welcome to macOS 27」欢迎窗口和 Finder，
   底部还会有快捷键提示。可以直接点「Skip tour」跳过。

---

## 三、Liquid Glass（液态玻璃）—— 本作的灵魂

这不是普通的毛玻璃，而是一套实时材质系统：

- **实时折射**：每扇窗口下方都有一个动态 SVG 位移场，像透镜一样**扭曲折射**壁纸；
- **自适应着色**：窗口每秒多次采样身后壁纸的颜色，自动“喝”进周围的色彩——
  把窗口拖到夕阳上，玻璃会变暖；拖到海面上，玻璃会变冷；
- **背景模糊 + 饱和度 + 亮度**叠加合成；
- **镜面高光描边**（1px 光线边缘）与随焦点流动的光泽。

**怎么玩**：

1. 打开 **System Settings（系统设置）→ Liquid Glass**；
2. 拖动「Blur radius / Saturation / Brightness / Tint strength」滑块——**整个系统实时变化**；
3. 试试预设：Default（默认）/ Heavy（厚重）/ Crisp（通透）/ Flat（扁平，关闭玻璃）；
4. 关闭「Refraction（折射）」可对比有无透镜效果；
5. 打开「Reduce Transparency（减弱透明度）」可切换到纯色平铺模式（省电、无障碍）。

其他入口：菜单栏 Apple 菜单 → Liquid Glass 分组；控制中心里的「Liquid Glass」开关；
终端里输入 `glass off` / `glass on`。

---

## 四、桌面与系统组件

| 组件 | 说明 |
| --- | --- |
| **菜单栏** | 左上角 Apple 菜单（关于本机、系统设置、睡眠、重启、关机、重置演示数据）；中间是当前应用的菜单；右侧依次是 Wi-Fi、电量、Spotlight、控制中心、时钟 |
| **程序坞 Dock** | 鼠标划过时图标**放大**；正在运行的应用下方有小圆点；点击废纸篓打开回收站；右键图标有快捷菜单 |
| **窗口管理** | 拖动标题栏移动（拖到屏幕边缘可**分屏吸附**：左半屏 / 右半屏 / 顶部最大化）；拖动边缘或四角缩放；双击标题栏缩放；红色 / 黄色 / 绿色按钮 = 关闭 / 最小化（飞向 Dock）/ 缩放 |
| **Spotlight** | 按 `⌘Space` 呼出，可搜索应用、文件、**直接算数学题**、执行系统命令、跳转网络搜索 |
| **控制中心** | 菜单栏最右侧双胶囊图标：Wi-Fi / 蓝牙 / 隔空投送 / 专注模式 / 深色模式 / Liquid Glass / 小组件开关，还有亮度、音量滑块和音乐控制 |
| **启动台 Launchpad** | 点击 Dock 上的火箭图标（或菜单 Window → Launchpad），全屏网格展示所有应用，支持搜索 |
| **调度中心 Mission Control** | 所有窗口**实时缩放**排布成网格，点击任意窗口聚焦 |
| **应用切换器** | 按住 `⌘Tab` 循环切换打开的应用（`⇧⌘Tab` 反向） |
| **小组件** | 桌面右侧：时钟（指针 + 数字）、日历、天气（天气会随壁纸变化！）。右键可编辑 / 移除 |
| **桌面图标** | Macintosh HD、Documents、欢迎文档、废纸篓；可拖动摆放；右键有菜单 |

---

## 五、内置应用一览

| 应用 | 能做什么 |
| --- | --- |
| **Finder 访达** | 真实的**虚拟文件系统**（持久化保存）。侧边栏收藏夹 / 图标网格 / 列表视图、前进后退、右键重命名、新建文件夹、拖拽移动文件、移到废纸篓、显示简介。双击打开文档和图片 |
| **Safari** | 多标签页浏览，内置收藏夹（维基百科、MDN 等）。页面在沙箱 iframe 中加载，**很多网站会拒绝被嵌入**，此时用页面上方的「Open ↗」按钮在新标签页打开 |
| **Notes 备忘录** | 搜索、置顶、最近删除、自动保存的富文本编辑器（`⌘N` 新建笔记） |
| **Mail 邮件** | 三栏式：文件夹（收件箱 / 星标 / 已发送 / 草稿 / 废纸篓）、列表、阅读窗格；可写信、回复、星标、归档删除（含 6 封预置邮件） |
| **Messages 信息** | iMessage 风格气泡聊天。Mom / Alex / Apple 会**显示输入中动画后自动回复**；联系人「Intelligence」由 DeepSeek V4 Pro 真实回答 |
| **Calendar 日历** | 月视图、今日红色圆圈、事件胶囊（超过 3 个显示 +n）、右侧面板可新增 / 删除事件 |
| **Photos 照片** | 12 张程序化生成的“照片”（渐变天空 + 山峦 / 波浪 / 沙丘），悬停显示标题，点开大图灯箱，可左右翻页、点赞收藏、Esc 退出 |
| **Music 音乐** | **Liquid Radio 生成式电台**：5 首完全用 WebAudio 实时合成（无任何音频文件）的曲目——琶音 + 低音 + 铺垫和声，带反馈延迟与混响，底部有**实时频谱可视化** |
| **Calculator 计算器** | 支持四则运算、括号、百分号，键盘可直接输入；Spotlight 里也能直接算 |
| **Terminal 终端** | 真·命令行，基于虚拟文件系统。常用命令见下文 |
| **TextEdit 文本编辑** | 打开 / 编辑 / 保存 `.txt` `.md` 文件（`⌘S` 保存） |
| **Preview 预览** | 查看图片，支持放大缩小、适应窗口 |
| **System Settings 系统设置** | 外观（浅色 / 深色 / 自动、强调色）、壁纸、Liquid Glass 工作室、Intelligence（DeepSeek 配置）、声音、关于 |
| **About This Mac 关于本机** | 含一块可点击的**实时液态玻璃演示卡片** |

### 终端常用命令

```bash
help                  # 命令列表
ls / cd / pwd / cat   # 浏览虚拟文件系统
open -a Finder        # 打开应用（也支持 open <文件名>）
mkdir / touch / rm    # 增删文件
neofetch              # 彩蛋：系统信息（含 DeepSeek V4 Pro）
wallpaper aurora      # 切换壁纸（tahoe/mammoth/aurora/sequoia/mono）
glass off             # 关闭液态玻璃
music play            # 控制音乐
deepseek <问题>       # 直接问 DeepSeek V4 Pro
say <文字>            # 语音朗读（浏览器语音合成）
sudo rm -rf /         # 会被 Tim 举报 😄
```

---

## 六、连接 DeepSeek V4 Pro（重点）

内置助手 **Intelligence** 出厂自带一个**离线演示大脑**，不配置也能聊天（比如让它解释
Liquid Glass、讲笑话）。要解锁完整模型：

1. 打开 **System Settings → Intelligence**；
2. 填写：
   - **Endpoint（端点）**：默认 `https://api.deepseek.com`，任何 OpenAI 兼容地址都可以；
   - **Model（模型）**：默认 `deepseek-v4-pro`；公共旗舰模型请选 `deepseek-chat`，
     推理模型选 `deepseek-reasoner`（若你的端点提供 v4-pro 就选它）；
   - **API key**：粘贴你的密钥（只保存在本浏览器 localStorage，**不会离开浏览器**，
     请求只发往你填写的端点）；
3. 点 **Test connection（测试连接）**，会显示延迟与连通结果；
4. 之后这些地方都会用上真实模型：
   - **Intelligence 应用**（Dock 上的发光圆球 / `⌘Space` 搜索 "intelligence"）——流式打字输出；
   - **Messages** 里的「Intelligence」联系人；
   - **Terminal** 里的 `deepseek <问题>` 命令。

---

## 七、快捷键速查

| 按键 | 功能 |
| --- | --- |
| `⌘Space`（Windows 为 `Ctrl+Space`） | Spotlight 搜索 |
| `⌘Tab` / `⌘⇧Tab` | 应用切换器 |
| `⌘W` / `⌘M` / `⌘N` | 关闭窗口 / 最小化 / 新建窗口 |
| `⌘,` | 打开系统设置 |
| `⌘⇧3` | **截图并保存到桌面**（真的会生成 Screenshot xx.png 文件） |
| `Esc` | 关闭所有浮层 / 菜单 |

> 说明：本项目面向 macOS 习惯设计，但在 Windows 上 `⌘` 会自动映射为 `Ctrl`。

---

## 八、数据存储与重置

- 所有数据都保存在浏览器的 **localStorage** 中，键名以 `macos27.` 开头
  （设置、虚拟文件系统、备忘录、邮件、信息、日历、照片点赞、聊天记录等）；
- 壁纸、音乐、照片、图标全部是**程序实时生成**，零外部文件；
- 想恢复出厂状态：**Apple 菜单 → Reset Demo Data…（重置演示数据）**，确认后清空全部数据并重启；
- 无痕模式 / 隐私模式下数据不会跨会话保留。

---

## 九、常见问题

**Q：页面打开是黑屏？**
A：等 3 秒开机进度条走完，再点一下锁定屏幕解锁。若仍是黑屏，请改用 Chrome / Edge。

**Q：玻璃效果看起来是纯色？**
A：浏览器不支持 `backdrop-filter`，或系统设置里「Enable Liquid Glass」被关闭了。
检查 System Settings → Liquid Glass。

**Q：Safari 里网页一片空白？**
A：正常现象——很多网站通过 `X-Frame-Options`/CSP 禁止被嵌入 iframe。点页面上方的
「Open ↗」在真实浏览器标签页中打开即可。

**Q：Intelligence 回答不了复杂问题？**
A：说明当前处于离线演示模式。按第六节配置 DeepSeek API key 即可。

**Q：音乐没声音？**
A：浏览器限制自动播放，第一次需要**点击播放按钮**（这是一个用户手势），之后即可流畅播放。

**Q：想关掉动画 / 省电？**
A：System Settings → Appearance → 打开「Reduce Motion（减弱动态效果）」；壁纸动画会停，
再配合「Reduce Transparency」可得到轻量模式。

---

祝你玩得开心 —— 记住：它不是磨砂，它是**液态**。✨
